<Window x:Class="TarkovPriceOverlay.OverlayPositionPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="348" Height="150" WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent" ShowInTaskbar="False"
        Topmost="True" ShowActivated="True">
  <Border Background="#E80B0D0C" BorderBrush="#D9BD77" BorderThickness="2"
          CornerRadius="4" MouseLeftButtonDown="DragSurface_MouseLeftButtonDown">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="5"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>
      <Border Background="#B99A55"/>
      <Grid Grid.Row="1" Margin="15,11,15,12">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel>
          <TextBlock DockPanel.Dock="Right" Text="拖动此框" Foreground="#D9BD77"
                     FontSize="11" VerticalAlignment="Center"/>
          <TextBlock Text="单物品价格窗口位置" Foreground="#E7E2D5"
                     FontSize="16" FontWeight="SemiBold"/>
        </DockPanel>
        <TextBlock Grid.Row="1" Margin="0,8,0,10" Text="按住框内空白处拖动到目标位置"
                   Foreground="#AAA79C" FontSize="11"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
          <Button Content="取消" Width="72" Height="28" Margin="0,0,8,0"
                  Click="Cancel_Click"/>
          <Button Content="保存位置" Width="88" Height="28" Click="Save_Click"/>
        </StackPanel>
      </Grid>
    </Grid>
  </Border>
</Window>
